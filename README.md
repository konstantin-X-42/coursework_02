# Курсовой проект 2: "Трекер самолётов". <img width="137" height="41" alt="Screenshot_4" src="https://github.com/user-attachments/assets/ac708293-d4d3-4222-980d-a89643052ae0" />

## Описание:
Проект на GitHub разработка по GitFlow.
Учьтены рекомендации PEP 8, используя линтеры и форматеры, сделаны атомарные коммиты.

## Установка зависимостей:
1. Клонируйте репозиторий:
    ```
    git clone https://github.com/konstantin-X-42/coursework_02
    ```
2. Установите зависимости, идентичные файлу poetry.lock если лока нет на основе pyproject.toml
    ```
    poetry install
    ```
------------------------------------------------------------------------------------------------------------------
## Установка библиотек:
1. Библиотека фреймворк ```pytest``` — зависимость для тестирования, проверки стиля кода или отладки, инструмент для написания автоматических тестов. Каманда, установка через инструмент для управления проектами и зависимостями — ```poetry```
    ```
    poetry add --group dev pytest
    ```
3. Библиотека ```pytest-cov``` — зависимость для тестирования, проводит анализ покрытия кода при проведении тестирования. Каманда, установка через ```poetry```
    ```
    poetry add --group dev pytest-cov
    ```
4. Библиотека ```requests``` — инструмент в Python для отправки HTTP-запросов в интернет. Каманда, установка через ```poetry```
    ```
    poetry add requests
    ```
5. Библиотека ```python-dotenv``` — безопасно разделяет написаный программный код и конфиденциальные данные (пароли, токены, ключи API), автоматически находит текстовый файл ```.env```.  Каманда, установка через ```poetry```
    ```
    poetry add python-dotenv
    ```
------------------------------------------------------------------------------------------------------------------
__ПРОЕКТ СОСТОИТ ИЗ 4 МОДУЛЕЙ:__
1. api.py — реализует паттерн «Адаптер» и абстрактный класс для работы со сторонними сетевыми API
   (Nominatim и OpenSky Network) с механизмом защиты от сетевых блокировок.
2. models.py — инкапсулирует данные о воздушном судне в объекты класса Aeroplane,
   осуществляет валидацию типов и перегрузку операторов сравнения (<, >, ==) для анализа скорости и высоты полёта.
3. storage.py — реализует паттерн «Репозиторий» для персистентного хранения информации в формате
   JSON внутри изолированной директории data/.
4. main.py — организует интерактивный CLI-интерфейс для конечного пользователя
   (сортировка ТОП-N, фильтрация по стране, удаление).

__СТРУКТУРА ЭЛЕМЕНТОВ:__
```
📁 data/ — папка с базой данных JSON.
📄 api.py — интеграция с внешними сервисами.
📄 models.py — объектные модели самолетов.
📄 storage.py — сохранение и фильтрация в файлах.
📄 main.py — запуск программы и меню пользователя.
📁 tests/ — пакет модулей автоматических тестов проекта.
```

__СТРУКТУРА ПРОЕКТА:__
```
coursework_02/
├── main.py
├── models.py
├── storage.py
├── api.py
├── data/
│
├── tests/
│   ├── conftest.py
│   ├── test_models.py
│   ├── test_storage.py
│   └── test_api.py
└── pyproject.toml
```


шаг 1
1. Создан модуль api.py    
2. Абстрактный класс BaseAPIAdapter(ABC), задает жесткую структуру.
3. Основной класс объявлен как class APIAdapter(BaseAPIAdapter): — это наследование в рамках паттерна Адаптер.
4. Получаем координаты через Nominatim
5. Если сервер не дал координаты (ошибка) используем режим офлайн - локальный справочник
6. Вывод координат
7. Отпрвляем запрос в OpenSky

шаг 2
1. Создан модуль models.py, по условию задания Шаг 2.
2. Реализовани класс: Aeroplane с валидацией входных данных
3. Программа оперирует не абстрактными индексами, а понятными свойствами объектов
   (plane.velocity, plane.altitude).
4. Реализована валидация (защита от значений None, когда самолет стоит на взлетной полосе).
5. Класс Aeroplane включает методы:
   - Сравнение ПО ВЫСОТЕ
   - Сравнение ПО СКОРОСТИ
     
шаг 3
1. Создан абстрактный класс BaseStorage и дочерний класс JsonFileStorage.
2. Реализовано сохранение объектов в физический файл flights_data.json на диске.
3. Реализован поиск по критериям (фильтрация по высоте/скорости) и удаление записей по позывному.

шаг 4
1. Консольный интерактивный интерфейс: Меню бесконечно крутится в цикле while True,
   пока пользователь явно не выберет пункт 5.
2. Запрос страны при старте: Автоматически подгружает и сохраняет свежие данные в data/flights_data.json.
3. ТОП N по высоте: Пользователь сам вводит число N, программа сортирует объекты самолётов и отдаёт самые высокие рейсы.
4. Фильтр по стране регистрации: Программа ищет совпадения по полю origin_country.
5. Дополнительно: Возможность удаления самолёта по позывному прямо из меню.
















## Использование:
#### **В проекте реализованы следующие функции в модулях:**

## **1. модуль ```main.py``` в пакете ```src```**
### ***Основной модуль запуска программы***
```
        main()
```
###### Аргументы функции:
```
    main()
```
###### функция работает в интерактивном режиме выводит запрограмированные варианты запросов
- курсы валют
- курс ценных бумаг
- Сервисы - простой поиск
- Отчёты траты по категории
##### В функцию интегрирована система логирования
------------------------------------------------------------------------------------------------------------------
## **2. модуль ```reports.py``` в пакете ```src```**
### ***Декортаор для функций отчётов***
```
        save_report_to_file()
```
    Декоратор для функций-отчетов.
    Если вызван без параметров (@save_report_to_file), сохраняет в файл по умолчанию.
    Если вызван с параметром (@save_report_to_file("my_report.xlsx")), сохраняет в указанный файл.
```
        spending_by_category()
```
    Возвращает траты по заданной категории за последние три месяца от переданной даты.
    Если дата не передана, то берется текущая дата.

##### В функции интегрирована система логирования
------------------------------------------------------------------------------------------------------------------
## **3. модуль ```services.py``` в пакете ```src```**
### ***Функции запросов курсов валют, ценных бумаг, транзакций***
```
    get_currency_rates()
```
    Принимает список валют (например, ['USD', 'EUR']) и возвращает
    список словарей с их курсами к рублю в формате:
    [{'currency': 'USD', 'rate': 91.5}, {'currency': 'EUR', 'rate': 98.2}]
###### адрес сервера API:
```https://marketplace.apilayer.com/```
```
    get_stock_prices()
```
    Запрос текущих цен акций в USD через бесплатный финансовый API.
    Принимает список тикеров акций (например, ['AAPL', 'AMZN'])
    и возвращает список словарей с их последней ценой закрытия (в USD)
###### адрес сервера API:
```https://api.marketstack.com/```
```
    analyze_cashback_categories()
```
    Анализирует транзакции за указанный год и месяц с использованием ФП.
    Рассчитывает потенциальный кешбэк (1% от суммы расходов) по каждой категории.
    Возвращает JSON-строку с результатами.

```
    simple_search()
```
    Ищет транзакции, содержащие поисковый запрос в описании или категории.
    Регистр символов не учитывается.

```
    search_by_phone_numbers()
```
    Возвращает транзакции, содержащие в описании мобильные телефонные номера.
    Поддерживает форматы: +7 9XX XXX-XX-XX, +79XXXXXXXXX, 89XXXXXXXXX и т.д.
##### В функции интегрирована система логирования
------------------------------------------------------------------------------------------------------------------
## **4. модуль ```utils.py``` в пакете ```src```**
### ***Функции обработки запросов пользователя***
```
        get_month_range()
```
Вычисляет диапазон от 1-го числа месяца до указанной даты.
```
        load_user_settings()
```
Читает пользовательские настройки из JSON-файла.
```
        get_greeting()
```
Возвращает приветствие строго по временным интервалам ТЗ.
```
        parse_incoming_datetime()
```
Парсит строку 'YYYY-MM-DD HH:MM:SS'.
Возвращает объект даты и дату начала этого месяца.
##### В функции интегрирована система логирования
------------------------------------------------------------------------------------------------------------------
## **5. модуль ```views.py``` в пакете ```src```**
### ***Функции формируют пакет данных по запросу пользователя***
```
        analytics_view()
```
Генерирует данные для ответа на основе переданной даты
```
        generate_json_response()
```
Генерирует данные по курсам валют и акциям на основе настроек пользователя
```
        generate_main_page_json()
```
Главная функция генерации JSON-ответа для страницы 'Главная'
```
        generate_main_page_data()
```
Формирует данные для Главной страницы согласно описанию колонок
##### В функции интегрирована система логирования
------------------------------------------------------------------------------------------------------------------
## **6. модуль ```test_get_greeting.py``` в пакете ```tests```**
### ***Тестирование логики функции: ```get_greeting()``` импортированной из модуля ```utils.py``` пакета ```src``` на ошибки и покрытие тестами***
```
        get_greeting()
```
------------------------------------------------------------------------------------------------------------------


## **7. модуль ```test_parse_incoming_datetime.py``` в пакете ```tests```**
### ***Тестирование логики функции: ```parse_incoming_datetime()``` импортированной из модуля ```utils.py``` пакета ```src``` на ошибки и покрытие тестами***
```
        parse_incoming_datetime()
```
------------------------------------------------------------------------------------------------------------------
## **8. модуль ```test_reports.py``` в пакете ```tests```**
### ***Тестирование логики функций, импортированных из модуля ```reports.py``` пакета ```src``` на ошибки и покрытие тестами***
```
        save_report_to_file()
```
```
        spending_by_category()
```
------------------------------------------------------------------------------------------------------------------
## **9. модуль ```test_services.py``` в пакете ```tests```**
### ***Тестирование логики функций, импортированных из модуля ```services.py``` пакета ```src``` на ошибки и покрытие тестами***
```
    get_currency_rates()
```
```
    get_stock_prices()
```
```
    analyze_cashback_categories()
```
```
    simple_search()
```
```
    search_by_phone_numbers()
```
------------------------------------------------------------------------------------------------------------------
## **10. модуль ```test_utils.py``` в пакете ```tests```**
### ***Тестирование логики функций, импортированных из модуля ```utils.py``` пакета ```src``` на ошибки и покрытие тестами***
```
        get_month_range()
```
```
        load_user_settings()
```
------------------------------------------------------------------------------------------------------------------
## **11. модуль ```test_views.py``` в пакете ```tests```**
### ***Тестирование логики функций, импортированных из модуля ```views.py``` пакета ```src``` на ошибки и покрытие тестами***
```
        analytics_view()
```
```
        generate_json_response()
```
```
        generate_main_page_json()
```
```
        generate_main_page_data()
```
------------------------------------------------------------------------------------------------------------------
## **12. директория размещения ```operations.xls``` c транзакциями:    ```data```**
### ***В эту жу папку сохраняются файлы выборки информации пользователем***
------------------------------------------------------------------------------------------------------------------
## **Структура модулей программы "Приложение для анализа банковских операций"**
```
.
├── src
│ ├── __init__.py
│ ├── utils.py
│ ├── main.py
│ ├── views.py
│ ├── reports.py
│ └── services.py
├── data
│ ├── operations.xlsx
├── tests
│ ├── __init__.py
│ ├── test_utils.py
│ ├── test_views.py
│ ├── test_reports.py
│ └── test_services.py
├── user_settings.json
├── .venv/
├── .env
├── .env_template
├── .git/
├── .idea/
├── .flake8
├── .gitignore
├── pyproject.toml
├── poetry.lock
└── README.md
```
------------------------------------------------------------------------------------------------------------------
## **Задачи реализованные в проекте:**
```
Веб-страницы:
    - Главная
    - События >>> функционал не реализован
Сервисы:
    - Анализ категорий повышенного кешбэка
    - Инвесткопилка >>> функционал не реализован
    - Простой поиск
    - Поиск танзакций по номерам телефонов
    - Поиск переводов физическим лицам >>> функционал не реализован
Отчеты:
    - Траты по категории
    - Траты по дням недели >>> функционал не реализован
    - Траты в рабочий/выходной день >>> функционал не реализован
```
------------------------------------------------------------------------------------------------------------------
## Тестирование

В проекте используется фреймворк `pytest` для проверки корректности работы функций и обработки ошибок.

### Запуск тестов
Команда выводит информацию в консоль, автоматически запускает все тесты в модулях (во всем проекте) с помощью фреймворка pytest внутри изолированного виртуального окружения Poetry.
```
poetry run pytest
```
Команда выводит информацию в консоль, автоматически запускает тесты в указанном модуле ```tests``` - папка с модулем теста, ```test_decorators.py``` - название модуля тестов
```
pytest tests/test_decorators.py
```
Команда выводит описание ошибки и информацию в консоль, автоматически запускает тесты в указанном модуле
```
pytest -s tests/test_decorators.py
```

### Анализ покрытия кода (Coverage)
Посмотреть, насколько полно код покрыт тестами, используйте `pytest-cov`:
```
poetry run pytest --cov=src
```
Команда автоматически запускает все тесты в проекте (из папки tests) и выводит в консоль сводную таблицу с процентом покрытия кода для модулей внутри директории ```src```
```
pytest --cov=src tests/
```
Команда автоматически запускает все тесты в проекте (из папки tests) и генерирует детальный интерактивный HTML-отчет с процентом покрытия кода для модулей внутри директории ```src```
```
pytest --cov=src --cov-report=html tests/
```
------------------------------------------------------------------------------------------------------------------
